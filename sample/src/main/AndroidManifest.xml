<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastMvvm"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".view.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.activity.DetailActivity"
            android:exported="false" />

        <activity
            android:name=".view.activity.NetworkActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.BottomNavigationAcitvity"
            android:exported="false"
            android:label="@string/title_activity_bottom_navigation_acitvity" />
        <activity android:name="com.fastmvvm.sample.view.activity.LoginActivity" />

    </application>

</manifest>